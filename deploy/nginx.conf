server {
    listen 80;
    listen [::]:80;
    # Кириллический домен + punycode (нужны оба варианта)
    server_name ублегко.рф www.ублегко.рф xn--90aefvcv3a.xn--p1ai www.xn--90aefvcv3a.xn--p1ai _;

    client_max_body_size 25m;

    location /static/ {
        alias /var/www/ublegko/staticfiles/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location /media/ {
        alias /var/www/ublegko/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
